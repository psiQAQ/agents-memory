# Task 5 deterministic Mock `full-6449fcf8` Ready

## 状态

**Ready / Not Run**。

这是 normal headless phase diagnostic `a666e597` Passed/cleaned 后的全新唯一 full-Mock tuple。历史 Ready、Blocked、Passed 与 cleanup records 均保持 append-only。本记录只证明静态合同和 freshness/preflight，不证明 17-step workload、三写六读、final、TUI 或真实模型。

## 固定输入

| 项目 | 值 |
| --- | --- |
| Root preflight HEAD | `3d9eff05333623a3fc593d2ccf29c24ac7910067`（clean） |
| Product/gitlink | `9e456a5b7bb47ae40596237d0f0b87c1edfc098f`（clean/exact） |
| `RUN_ID` | `task5-mock-20260812-full-6449fcf8` |
| `COMPOSE_PROJECT_NAME` | `refine-memory-task5-mock-20260812-full-6449fcf8` |
| `EVIDENCE_DIR` | `D:\workspace\refine-memory\.worktrees\four-agent-memory\.runtime\runs\task5-mock-20260812-full-6449fcf8` |

- exact Compose project labels 的 containers/networks/volumes 为 `0/0/0`；global containers `0`；evidence path 不存在且 parent 为普通目录；`127.0.0.1:8125` listeners `0`。
- active image refs `7/7` 与固定 IDs 精确匹配；base/mock/claude/opencode/pi/management 合并配置 Passed，runtime Compose 不含 build。
- root Node `254/254`、active Compose matrix `7/7`、normal headless diagnostic overlay `1/1` 与独立 review `C0/I0/M0` 已在前一阶段通过。
- tracked launcher 固定 17 步：services、bootstrap、三 config、protocol/leak、management、三 write、六 read、finalize；首个非零只输出 `step=1..17`，不转发 child stdout/stderr。

## 唯一允许的下一步

只允许 tracked `tests/integration/tools/run-task5-mock.mjs` 对该 tuple 执行一次。禁止 build、retry、复用旧 tuple、读取 raw child/log/evidence、TUI 或真实/Paid 模型。

若 freshness 通过且本次运行创建了 exact project，结果后先做脱敏计数盘点，再只对该 project 使用 base/mock/claude/opencode/pi/management overlays 与对应 profiles 执行 `down --volumes --remove-orphans`；不得使用 `--rmi` 或全局 prune。若在 label collision 或其他 preflight 阶段失败，则保留碰撞或未知资源审计，不执行 cleanup。
