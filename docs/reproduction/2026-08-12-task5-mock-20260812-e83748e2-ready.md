# Task 5 deterministic Mock `e83748e2` Ready

## 状态

**Ready / Not Run**。

这是 `113ca669` single Claude diagnostic Passed/cleaned 后的全新唯一 full Mock tuple。它只允许 tracked `tests/integration/tools/run-task5-mock.mjs` 执行固定 17 步，不复用任何历史 project/evidence。

## 固定输入与 freshness

| 项目 | 值 |
| --- | --- |
| Root HEAD | `3ee4772`（clean） |
| Product gitlink | `9e456a5b7bb47ae40596237d0f0b87c1edfc098f`（clean/exact） |
| `RUN_ID` | `task5-mock-20260812-e83748e2` |
| `COMPOSE_PROJECT_NAME` | `refine-memory-task5-mock-20260812-e83748e2` |
| `EVIDENCE_DIR` | `D:\workspace\refine-memory\.worktrees\four-agent-memory\.runtime\runs\task5-mock-20260812-e83748e2` |

- exact project labels 为 containers/networks/volumes `0/0/0`；evidence path 不存在；global containers `0`；`127.0.0.1:8125` listeners `0`。
- base + mock/claude/opencode/pi/management merged config Passed；required exact image refs `7/7` 可 inspect。
- root `232/232`、Compose config `7/7`、single Claude diagnostic 与 EROFS/tmpfs runtime asset 均已 Passed。

## 唯一允许的运行

launcher 固定执行 long-running services → bootstrap → 三 client config → 24-case protocol/leak → management/outsider → Claude/OpenCode/Pi 三次 write → 六次 ordered cross-owner read → final oracle。任一步失败立即停止，不 retry、不读 raw logs/evidence、不进入 TUI/real。

若完整 Gate 成功，exact project 保留到 scoped review 与用户 TUI 人工确认；这时它尚未“用完”，不得提前 cleanup。若失败，则先做最小脱敏诊断/归档，再按用户授权精确清理该 project containers/network/volumes；不得使用 `--rmi` 或 prune。
