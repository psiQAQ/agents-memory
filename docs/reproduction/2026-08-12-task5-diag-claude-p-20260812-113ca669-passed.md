# Task 5 Claude `-p` diagnostic `113ca669` Passed

## 状态

**Runtime Passed（single Claude diagnostic only）**。

tracked coordinator 对 [113ca669 Ready](2026-08-12-task5-diag-claude-p-20260812-113ca669-ready.md) 固定的全新 tuple 仅运行一次并以 `0` 退出。宿主只接收 coordinator 校验后的 canonical 18-key JSON：

```json
{"status":"classified","launch":"code0","launch_phase":"cli-zero","launch_category":"none","output_present":true,"proxy_dns_ok":true,"proxy_tcp_ok":true,"continuity":"ok","sequence_delta":1,"total_delta":1,"expected_operation_present":true,"expected_operation_valid":true,"expected_main_count":1,"unexpected_operation_count":0,"unexpected_path_count":0,"unsafe":false,"dropped":0,"truncated":false}
```

## 已证明边界

- Claude Code `2.1.226` 在 replacement image 与 hardened `/tmp` tmpfs 下正常 exit `0`。
- Proxy DNS/TCP probes 均通过；Mock aggregate continuity 为 `ok`。
- Mock 精确观察到一个预期 Claude write 主请求；operation、marker、path 与 main count 合同均有效。
- 无额外 operation/path、无 sensitive observation、无 dropped 或 truncated state。

未读取 raw child output、runtime logs、settings、home、credential、identity 或 evidence 内容。该结果只证明单次 Claude diagnostic write，不证明完整 protocol/leak、management/outsider、三写六读、final oracle、三个正式 headless、TUI 或真实/Paid 模型。
