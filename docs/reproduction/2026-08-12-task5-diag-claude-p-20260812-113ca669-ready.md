# Task 5 Claude `-p` diagnostic `113ca669` Ready

## 状态

**Ready / Not Run**。

这是 EROFS classifier 与 hardened tmpfs fix 后的全新唯一 diagnostic tuple。历史 Ready/Blocked/cleanup records 保持 append-only；本记录只证明 freshness 与静态/镜像输入，不证明 Docker workload 或业务 Gate。

## 固定输入与 preflight

| 项目 | 值 |
| --- | --- |
| Root HEAD | `f56e5e2`（clean） |
| Product gitlink | `9e456a5b7bb47ae40596237d0f0b87c1edfc098f`（clean/exact） |
| `RUN_ID` | `task5-diag-claude-p-20260812-113ca669` |
| `COMPOSE_PROJECT_NAME` | `refine-memory-task5-diag-claude-p-20260812-113ca669` |
| `EVIDENCE_DIR` | `D:\workspace\refine-memory\.worktrees\four-agent-memory\.runtime\runs\task5-diag-claude-p-20260812-113ca669` |
| Claude image | `refine-memory-claude-code:2.1.226@sha256:261a917376f791d9b5e092040c2f488f23588b7103a27606226426f273b040dd` |

- exact Compose project labels 为 containers/networks/volumes `0/0/0`；evidence path 不存在；global containers `0`；`127.0.0.1:8125` listeners `0`。
- diagnostic 所需 image refs 为 `5/5`；merged diagnostic config、new Claude digest、bounded hardened tmpfs、read-only diagnostic bind 与 tracked coordinator contract 均 Passed。
- classifier/tmpfs 修复、单次 image build 与资产证据见 [Claude EROFS/tmpfs fix Passed](2026-08-12-task5-claude-erofs-tmpfs-fix-passed.md)。

## 唯一允许的下一步

只允许 tracked `tests/integration/tools/run-task5-claude-diagnostic.mjs` 对本 tuple 执行一次 canonical 18-key diagnostic。禁止 retry、复用旧 tuple、raw child/log/evidence 读取、TUI 或真实/Paid 模型。完成后必须另增 append-only result/cleanup 记录，并仅清理 exact project 的 containers、network 与 volumes；active images 保留到不再被配置引用且确认无容器引用时再精确删除。
