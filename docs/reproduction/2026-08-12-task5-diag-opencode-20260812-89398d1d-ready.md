# Task 5 OpenCode typed diagnostic `89398d1d` Ready

## 状态

**Ready / Not Run**。

这是 `opentitle-4f056ee6` fixed step 9 Blocked 并 exact cleanup 后的全新唯一 diagnostic tuple。历史 Ready/Blocked/cleanup records 保持 append-only；本记录只证明 typed diagnostic 的静态合同、review 与 freshness/preflight，不证明 Docker workload、完整 Mock 或业务 Gate。

## 固定输入与 preflight

| 项目 | 值 |
| --- | --- |
| Root HEAD / diagnostic code | `c163b6c83052688476cbfe1b102151e1d694f2c3`（clean） |
| Product/gitlink | `9e456a5b7bb47ae40596237d0f0b87c1edfc098f`（clean/exact） |
| `RUN_ID` | `task5-diag-opencode-20260812-89398d1d` |
| `COMPOSE_PROJECT_NAME` | `refine-memory-task5-diag-opencode-20260812-89398d1d` |
| `EVIDENCE_DIR` | `D:\workspace\refine-memory\.worktrees\four-agent-memory\.runtime\runs\task5-diag-opencode-20260812-89398d1d` |
| OpenCode image | tag `refine-memory-opencode:1.18.16`；image ID `sha256:263a6d0eade24b72b4b2627984a930fc69a3e621519b1ec050a0320398b890a1` |

- TDD RED 为两个缺失 export；GREEN 后 focused diagnostic suites `60/60`、root Node `239/239`、active Compose matrix `7/7`、OpenCode diagnostic overlay `1/1`，独立 review 为 `Critical 0 / Important 0 / Minor 0`。
- exact Compose project labels 为 containers/networks/volumes `0/0/0`；global containers `0`；evidence path 不存在且 parent 是普通目录；`127.0.0.1:8125` listeners `0`。
- active image refs 为 `7/7`；OpenCode diagnostic overlay merged Compose config Passed。以上均为静态/freshness 输入，不是 workload 或业务结果。

## 唯一允许的下一步

只允许 tracked `tests/integration/tools/run-task5-opencode-diagnostic.mjs` 对本 tuple 执行一次 canonical 18-key diagnostic。禁止 build、retry、复用旧 tuple、full Mock、raw child/log/evidence 读取、TUI 或真实/Paid 模型。若 coordinator 已通过 freshness 检查并由本次运行创建 exact project，结果后必须新增 append-only result/cleanup 记录，并只对该 exact project 使用对应 base/mock/opencode/diagnostic overlays 与 profiles 执行 `down --volumes --remove-orphans`；若在 label collision 或其他 preflight 阶段固定失败，则保留碰撞/未知资源并先审计，不执行 cleanup。任何路径均不得使用 `--rmi` 或全局 prune。
