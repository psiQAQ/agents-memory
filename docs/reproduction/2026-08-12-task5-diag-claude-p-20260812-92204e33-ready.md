# Task 5 Claude `-p` diagnostic `92204e33` Ready

## 状态

**Ready / Not Run**。

这是 replacement Claude classifier image 固定后创建的全新唯一 diagnostic tuple。它不复用既有失败现场，不覆写任何历史 Ready/Blocked 记录，也不把 preflight、镜像检查或静态配置写成 Claude/业务 Gate 已通过。

## 固定输入与 freshness

| 项目 | 值 |
| --- | --- |
| Root HEAD | `6e7d695e882acdd06f94e104a72d278f3b6f5080`（clean） |
| Product gitlink | `9e456a5b7bb47ae40596237d0f0b87c1edfc098f`（clean/exact） |
| `RUN_ID` | `task5-diag-claude-p-20260812-92204e33` |
| `COMPOSE_PROJECT_NAME` | `refine-memory-task5-diag-claude-p-20260812-92204e33` |
| `EVIDENCE_DIR` | `D:\workspace\refine-memory\.worktrees\four-agent-memory\.runtime\runs\task5-diag-claude-p-20260812-92204e33` |
| Claude image | `refine-memory-claude-code:2.1.226@sha256:058eccaf56507941c27fd1ce57e69cb6ae5cff20680e7a36ed80bddb22ec946b` |

- exact Compose project labels 为 containers/networks/volumes `0/0/0`，evidence path 不存在，global containers 为 `0`，`127.0.0.1:8125` listener 为 `0`。
- active images 的 exact inspect 为 `7/7`；diagnostic headless 使用上表 Claude digest，entrypoint、只读 bind、非 root UID/security 与 tracked coordinator contract 均通过 preflight。
- 这些只是 freshness、静态合同和镜像输入证明；Docker workload、Claude write、Proxy/Core、Mock observation 与 canonical 18-key diagnostic output 均为 **Not Run**。

## 唯一允许的下一步

只允许由 tracked `tests/integration/tools/run-task5-claude-diagnostic.mjs` 对本 tuple 执行一次固定诊断。宿主只接受 coordinator 校验后的 canonical 18-key JSON，不读取或输出 raw child output、error message、URL、IP、path、hash、credential、identity、settings、home、日志或原始 evidence。

本 tuple 不允许 retry、reset、finalize、TUI、真实/Paid 模型或范围外命令。诊断完成后另增 append-only reproduction，并按精确 project allowlist 清理该 tuple 的 containers、network 与 volumes；保留 host evidence 和仍被 active 配置引用的镜像。
