services:
  claude-config:
    image: refine-memory-integration-tools:task5
    profiles: ["claude"]
    user: "0:0"
    depends_on:
      bootstrap:
        condition: service_completed_successfully
    command: ["tools/prepare-agent.mjs", "--agent", "claude", "--state-dir", "/state/run", "--home-dir", "/agent-home", "--space-id", "${MEMORY_SPACE_ID:-default}"]
    volumes:
      - bootstrap-state:/state:ro
      - claude-home:/agent-home
    restart: "no"
  claude-client:
    image: refine-memory-claude-code:2.1.226
    profiles: ["claude"]
    user: "10001:10001"
    init: true
    stdin_open: true
    tty: true
    read_only: true
    cap_drop: ["ALL"]
    security_opt: ["no-new-privileges:true"]
    depends_on:
      claude-config:
        condition: service_completed_successfully
      memory-proxy:
        condition: service_healthy
    environment:
      HOME: /home/agent
      MEMORY_SPACE_ID: ${MEMORY_SPACE_ID:-default}
    volumes:
      - claude-home:/home/agent
      - claude-workspace:/workspace
    command: ["--version"]
  claude-headless:
    image: refine-memory-claude-code:2.1.226
    profiles: ["claude"]
    user: "10001:10001"
    init: true
    read_only: true
    cap_drop: ["ALL"]
    security_opt: ["no-new-privileges:true"]
    depends_on:
      claude-config:
        condition: service_completed_successfully
      memory-proxy:
        condition: service_healthy
    environment:
      HOME: /home/agent
      MEMORY_SPACE_ID: ${MEMORY_SPACE_ID:-default}
      MOCK_BASE_URL: http://mock-llm:8080
      STAGE1_CLIENT_SCENARIO: ${STAGE1_CLIENT_SCENARIO:-write}
      STAGE1_OWNER: ${STAGE1_OWNER:-}
    volumes:
      - claude-home:/home/agent
      - claude-workspace:/workspace
      - claude-evidence:/client-evidence
    entrypoint: ["node", "/opt/memory-client/task5-headless-client.mjs"]
    command: ["--client", "claude", "--run-id", "${RUN_ID:-task5-static}", "--home-dir", "/home/agent", "--bundle-file", "/home/agent/.memory/agent-bundle.json", "--space-id", "${MEMORY_SPACE_ID:-default}", "--template", "/opt/memory-client/settings.template.json", "--evidence-dir", "/client-evidence"]
    restart: "no"
