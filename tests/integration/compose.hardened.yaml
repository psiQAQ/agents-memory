services:
  memory-proxy:
    ports:
      - "127.0.0.1:8096:8096"
    volumes:
      - runtime-config:/runtime-config:ro
      - proxy-data:/data/tdai-memory-proxy
      - proxy-logs:/app/logs

volumes:
  proxy-data: {}
  proxy-logs: {}
